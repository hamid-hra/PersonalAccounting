# ---- Database ----
# این فایل داخل مخزن است تا پلتفرم بدون تنظیم دستی بالا بیاید.
# دیتابیس فقط از داخل شبکهٔ خود compose در دسترس است (پورتش بیرون باز نیست).
# اگر مخزن را عمومی کردی، این رمز را عوض کن.
POSTGRES_USER=accounting
POSTGRES_PASSWORD=J4X2uzSKCr1NA4uQDa8rtWMXS297XAx6
POSTGRES_DB=accounting
POSTGRES_HOST=db

# ---- اختیاری ----
# مدت اعتبار نشست (روز)
SESSION_DAYS=30
# کلید امضای کوکی نشست؛ خالی بماند، خودش تصادفی می‌سازد و در دیتابیس نگه می‌دارد
SECRET_KEY=

# رمز ورود در اولین اجرا از داخل خود برنامه ساخته می‌شود (صفحهٔ «ساخت حساب»).
# کلید سرویس قیمت (BrsApi) هم از صفحهٔ تنظیمات وارد می‌شود. هیچ‌کدام این‌جا نیست.
# رمز را فراموش کردی؟   docker compose exec app python -m app.reset_password
